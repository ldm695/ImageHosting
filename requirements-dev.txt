# Development / test dependencies
-r requirements.txt
pytest>=8.0

# Linter + formatter (ESLint/Prettier equivalent). Config lives in pyproject.toml.
#   ruff check .          lint
#   ruff check . --fix    auto-fix
#   ruff format .         format
ruff>=0.15

# Static type checker (like TypeScript's tsc). Config in pyproject.toml.
#   mypy
mypy>=1.8

# Browser E2E (run: pytest e2e). After install, fetch the browser once:
#   playwright install chromium
pytest-playwright>=0.5

# Font subsetting + woff2 (run: python scripts/subset_font.py). brotli powers
# woff2 compression. Only needed to regenerate the UI font, not at runtime.
fonttools>=4.60
brotli>=1.1
